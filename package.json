{
  "name": "deadshotio_client",
  "version": "1.0.1",
  "main": "index.js",
  "scripts": {
    "start": "electron .",
    "package": "electron-packager . Deadshot.ioClient --platform=win32 --arch=x64 --out=dist --overwrite --asar=false  --icon=icon/icon.png",
    "package:cli": "node scripts/package.js",
    "package:win": "node scripts/package.js --platform=win32 --arch=x64 --icon=icon/icon.png"
  },
  "author": "Sphygnoman",
  "license": "ISC",
  "description": "A desktop client for Deadshot.io, a web-based game.",
  "devDependencies": {
    "electron": "^42.2.0",
    "electron-packager": "^17.1.2"
  }
}
